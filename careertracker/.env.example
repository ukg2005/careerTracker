# Copy this to .env and fill in values for local dev.
# On Railway, set these as environment variables in the dashboard.

SECRET_KEY=your-secret-key-here
DEBUG=False
ALLOWED_HOSTS=your-railway-domain.up.railway.app

# Railway PostgreSQL — Railway injects this automatically
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Your deployed Vercel frontend URL (no trailing slash)
CORS_ALLOWED_ORIGINS=https://your-app.vercel.app

# Gmail SMTP (or any SMTP provider)
EMAIL_HOST_USER=you@gmail.com
EMAIL_HOST_PASSWORD=your-app-password
